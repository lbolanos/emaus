/**
 * Public contact details shown on the landing page.
 *
 * The YouTube channel is deliberately NOT linked from here: except for the
 * registration walkthrough, its videos cover running the system and are aimed
 * at the serving team, who reach them from the in-app help behind login.
 */

/** Project mailbox for public contact links (same Google account as the channel). */
export const CONTACT_EMAIL = 'emaus.cccc@gmail.com';
